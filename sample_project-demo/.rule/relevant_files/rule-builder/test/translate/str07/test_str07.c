#include <stdio.h>
#include <string.h>

#define BUFF_SIZE 2048

void func_err() {
    char msg[] = "MESSAGE1";
    char msg2[] = "MESSAGE2";
    char book[BUFF_SIZE]

    strcpy(book, msg2);
    strcat(msg, msg2);
}